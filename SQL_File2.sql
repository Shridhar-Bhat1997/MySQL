CREATE DATABASE sample;

CREATE TABLE employee # To create table
(
empid int primary key,
name varchar(30),
dob datetime,
email varchar(20)
);

DESC employee; # DESCRIBE command is used to dipslay the information about a table.